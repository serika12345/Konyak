interface:
  display_name: "Konyak /docs-only"
  short_description: "Edit Konyak docs or policy only"
  brand_color: "#0F766E"
  default_prompt: "Use $konyak-docs-only to run Konyak's /docs-only workflow."

policy:
  allow_implicit_invocation: false
