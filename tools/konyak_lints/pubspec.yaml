name: konyak_lints
description: Custom lint rules for Konyak repository policy.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.4

dependencies:
  analyzer: ^8.0.0
  custom_lint_builder: ^0.8.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
